# 多因子选股 ML 分类模型 notebook 依赖清单
# 运行环境：Python 3.13（项目 managed 版本）
# 安装：在 venv 中执行  pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0          # 含 nbconvert / ipykernel，用于运行 .ipynb

# 可选（GBDT 高性能对照，notebook 已做缺失容错）
xgboost>=2.0
lightgbm>=4.0
